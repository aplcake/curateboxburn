# Vercel environment variables for burn site

# WalletConnect (get from cloud.walletconnect.com)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Railway API URL (no trailing slash)
NEXT_PUBLIC_API_URL=https://your-api.railway.app

# Server-side: proxying admin calls to Railway
API_URL=https://your-api.railway.app
# Note: the admin key is NOT an env var here — you enter it in the /admin
# panel (same value as Railway's ADMIN_API_KEY) and it's sent per-request.

# Comma-separated admin wallet addresses (client-side UI gate)
NEXT_PUBLIC_ADMIN_WALLETS=0xYourWallet1,0xYourWallet2

# FCFS pool destination. This must be the same address as MINTER_PRIVATE_KEY
# on Railway, because that wallet returns late deposits and batch-burns slots.
NEXT_PUBLIC_POOL_WALLET=0x853E60bd61BDeB1E02dd3a35E0E621D0A0dD239F
